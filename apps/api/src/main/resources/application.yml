server:
  port: 8080

spring:
  application:
    name: luxury-enterprises-api

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
